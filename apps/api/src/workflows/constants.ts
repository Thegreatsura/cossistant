// Workflow notification constants

// Visitor message notification delay in minutes
// This delay allows grouping multiple messages into a single email
export const VISITOR_MESSAGE_DELAY_MINUTES = 1;

// Maximum number of messages to display in email notification
export const MAX_MESSAGES_IN_EMAIL = 3;
